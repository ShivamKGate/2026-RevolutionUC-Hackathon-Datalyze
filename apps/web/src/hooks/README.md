# Hooks Directory

Custom React hooks that are shared across multiple features.

## Typical Files

- Query hooks (`usePipelineStatus`, `useInsights`)
- Streaming hooks (`useAgentLogsSSE`)
- Local UI state hooks
