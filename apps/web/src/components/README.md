# Components Directory

Shared reusable UI components should live here.

## Typical Files

- Buttons, cards, badges, dialogs, table wrappers.
- Generic loading and error states.
- Layout primitives (`PageShell`, `Sidebar`, `TopBar`).
