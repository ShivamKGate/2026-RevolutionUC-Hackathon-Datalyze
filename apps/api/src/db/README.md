# Database Directory

Persistence-layer assets for the API.

## Intended Structure

- `migrations/`: Alembic migration files.
- `seeds/`: local seed scripts and data snapshots.
- `session.py` (future): DB engine/session factories.
