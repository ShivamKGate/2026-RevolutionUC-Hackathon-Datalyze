# Shared Package (`packages/shared`)

Cross-app contracts used by both frontend and backend.

## What Should Live Here

- Shared TypeScript interfaces for API contracts.
- Domain constants/enums mirrored across apps.
- Utility validation schemas when dual-runtime support is needed.
