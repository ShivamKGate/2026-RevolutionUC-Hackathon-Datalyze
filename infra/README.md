# Infrastructure

Deployment and infrastructure-as-code assets.

## Subdirectories

- `docker/`: Dockerfiles and image build context.
- `compose/`: docker compose stacks for local services.
- `terraform/`: infra provisioning assets.
